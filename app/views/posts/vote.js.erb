<% if @vote.valid? %>
	$("#post_<%= @post.slug %>_votes").html("<%= @post.total_votes %>" + " <%= @post.total_votes.abs == 1 ? 'vote' : 'votes' %>");
<% else %>
	alert('You can only vote on a post once.');
<% end %>