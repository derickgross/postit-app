<% if @vote.valid? %>
	$("#comment_<%= @comment.id %>_votes").html("<%= @comment.total_votes %>" + " <%= @comment.total_votes.abs == 1 ? 'vote' : 'votes' %>");
<% else %>
	alert('You can only vote on a comment once.');
<% end %>